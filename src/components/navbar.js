import React from 'react';
import { Link as ScrollLink} from 'react-scroll';

const Navbar = () => {
    return (
        
        <nav className="navbar">
            <div>Navbar</div>
            <button className="menu">
                <img src="" alt="" className="menu" /> Contact me!
            </button>
        </nav>
    )
}

export default Navbar;